export enum ReminderRecurrence {
  NONE = 'none',
  WEEKLY = 'weekly',
  MONTHLY = 'monthly',
  YEARLY = 'yearly',
}

export enum ReminderTimeMode {
  ALL_DAY = 'all_day',
  TIME = 'time',
  RANGE = 'range',
}

export enum ReminderNotifyUnit {
  HOURS = 'hours',
  DAYS = 'days',
}
